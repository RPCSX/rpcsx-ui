{
  "name": "rpcsx-ui",
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "main": "build/main.js",
  "scripts": {
    "build": "bun run --cwd rpcsx-ui-kit build && bun run --cwd rpcsx-ui build",
    "validate": "tsc -b rpcsx-ui",
    "start": "bun run --cwd rpcsx-ui start"
  },
  "license": "GPL-3.0-only",
  "workspaces": [
    "rpcsx-ui-kit",
    "rpcsx-ui"
  ]
}
